/**
 * Hidden utility pages: short list for cross-navigation (not in main nav).
 */
module.exports = () => ({
  siblings: [
    { url: '/type/', label: '/type' },
    { url: '/color/', label: '/color' },
    { url: '/ogimages/', label: '/ogimages' }
  ]
});
